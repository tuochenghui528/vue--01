import { createApp } from 'vue'
import App from './App.vue'

createApp(App).mount('#app')

//1.执行vue ui命令可以本地起GUI（图形界面）服务,来查看配置
//2.node_modules中的@vue->cli-service->webpack.config.js
//3.项目中创建vue.config.js(固定名字)来写配置module.exports={}
